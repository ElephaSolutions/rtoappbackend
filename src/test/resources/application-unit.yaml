spring:
  liquibase:
    change-log: "classpath:/db/changelog/db.changelog-test.yaml"
  jpa:
    properties:
      hibernate:
        integration:
          envers:
            enabled: false
message-service:
  host: message-api-sample.com
  api-key: another-mock-key
  license-number: another-mock-credential